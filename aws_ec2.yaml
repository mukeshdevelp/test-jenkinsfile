plugin: amazon.aws.aws_ec2
regions:
  - eu-west-1
filters:
  "tag:Environment": "staging"
keyed_groups:
  - key: tags.Name
    prefix: tag_name_
hostnames:
  - private_ip_address
compose:
  ansible_host: private_ip_address
